// Arrays

// Arrays in JS are different than other languages because
// Both length and type can be dynamic

// Arrays are of object type

let courses = ['hld', 'lld', 'dsa', 6, true, null];

console.log(courses);

console.log(courses[0]);
console.log(courses[1]);
console.log(courses[5]);



console.log(typeof(courses));
